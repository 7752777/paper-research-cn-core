# 图表硬规则

一篇证据图谱型论文应控制为少量高信息密度图表。图展示结构和流程，表展示精确数值与编码规则；同一组数字不应在图、表和正文重复三遍。每个图表必须可由冻结台账重算，具有编号、题注、分母、统计口径、数据来源和多重编码限制。流程图的去向必须与样本表和方法一致。

优先保存 SVG 或 PDF 源图，同时输出 PNG 预览。表格采用三线表，不使用装饰性色块、渐变、三维效果或不可打印的小字号。
